import Icon from "public/svg/noto_house.svg";
import { IconBaseProps } from "react-icons";

const SVGHome: React.FC<IconBaseProps> = (props) => {
  return <Icon {...props}></Icon>;
};
export default SVGHome;

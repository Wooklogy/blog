import Icon from "public/svg/noto_bookmark.svg";
import { IconBaseProps } from "react-icons";

const SVGPortfolio: React.FC<IconBaseProps> = (props) => {
  return <Icon {...props}></Icon>;
};
export default SVGPortfolio;
